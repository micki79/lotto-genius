name: 🍀 LottoGenius Multi-KI Analyse

on:
  schedule:
    - cron: '0 19 * * 3'
    - cron: '0 20 * * 6'
    - cron: '0 2 * * 0'
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      HUGGINGFACE_API_KEY: ${{ secrets.HUGGINGFACE_API_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      TOGETHER_API_KEY: ${{ secrets.TOGETHER_API_KEY }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
    
    steps:
      - name: 📥 Repository auschecken
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Dependencies installieren
        run: pip install requests numpy

      - name: 🎰 Lotto-Daten aktualisieren
        run: python scripts/fetch_data.py

      - name: 📊 KI-Analyse durchführen
        run: python scripts/analyze.py

      - name: 🧠 Aus Vorhersagen lernen
        run: python scripts/learn.py

      - name: 🔮 Neue Multi-KI Vorhersagen generieren
        run: python scripts/predict.py

      - name: 📤 Änderungen committen
        run: |
          git config --local user.email "lotto-genius-bot@github.com"
          git config --local user.name "🍀 LottoGenius Bot"
          git add -A
          if git diff --quiet && git diff --staged --quiet; then
            echo "Keine Änderungen"
          else
            git commit -m "🤖 Auto-Update: $(date '+%Y-%m-%d %H:%M')"
            git push
          fi
